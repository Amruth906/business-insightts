services:
  - type: web
    name: business-seo-insights-backend
    env: node
    plan: free
    rootDir: business-seo-insights-dashboard-main/backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
